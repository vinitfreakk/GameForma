<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AllAboutValorant" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/valo_balck</item>
        <item name="android:navigationBarColor">@color/valo_balck</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="CustomBottomNavTextAppearance" parent="TextAppearance.AppCompat.Caption">
        <item name="android:fontFamily">@font/valofont</item>
    </style>
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <!--<item name="android:color">@color/valo_red</item>-->
        <item name="android:color">@android:color/transparent</item>
    </style>

    <style name="Theme.AllAboutValorant" parent="Base.Theme.AllAboutValorant" />

</resources>